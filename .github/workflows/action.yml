name: Git_Final
on:
  push:
    branches:
        - main

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:

      - name: print hello
        run: echo "Hello"

      - name: ubuntu version
        run: lsb_release -a

      - name: print sha
        run: |
          echo $GITHUB_SHA
          

  job-2:
     runs-on: ubuntu-latest
     steps:
       - name: print files
         run: |
           cd ..
           pwd
           ls -la
           cat 1.txt
           cat 2.txt
         
       - name: run file
         run: ./test.sh

  job-3:
    runs-on: ubuntu-latest
    steps:

       - name: print  name
         env: |
           LastName: ${{ secrets.LAST_NAME }}
           

         run: |
            echo $LastName     
            echo 'Hi ${{ env.FIRST_NAME }}'     

       
         




       
